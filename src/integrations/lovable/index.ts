// Servantra custom auth helpers (replaced  integration)
// Google OAuth is handled directly via Supabase in Login.tsx

export const servantraAuth = {
  // Placeholder for any future custom auth utilities
};
